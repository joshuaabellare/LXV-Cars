<div class="slider-wrapper">
	<div class="three-item-carousel owl-carousel owl-loaded owl-drag display-block no-margin">
		<?php
		$arr_rows = $view->style_plugin->rendered_fields;
		foreach ($arr_rows as $key => $arr_row):
		?>
			<?php print $arr_row['nothing']; ?>

		<?php endforeach; ?>
	</div>
</div>